import 'package:flutter/material.dart';

class WrapperRole extends StatelessWidget {
  const WrapperRole({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //stream builder for firebase

    // if user role  == admin
    //return the admin Screen

    //if the user role == user
    //return user screen

    // if the user role == guest
    //return user screen
    return (Text("Wrapper Role"));
  }
}
